{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dialog-main",
  "description": "Native pickers and prompts in the main window. fs plugin still gates the resulting paths.",
  "windows": ["main"],
  "permissions": [
    "dialog:allow-open",
    "dialog:allow-save",
    "dialog:allow-message",
    "dialog:allow-ask",
    "dialog:allow-confirm"
  ]
}
