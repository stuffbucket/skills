{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater",
  "description": "Allow the main window to check for, download, and install updates, then relaunch the app.",
  "windows": ["main"],
  "permissions": [
    "updater:default",
    "process:default"
  ]
}
