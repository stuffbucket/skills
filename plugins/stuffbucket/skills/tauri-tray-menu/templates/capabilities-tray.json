{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the menu-bar tray app.",
  "windows": ["main", "popover"],
  "permissions": [
    "core:default",
    "positioner:default"
  ]
}
