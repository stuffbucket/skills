{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "sql-main",
  "description": "Allow the main window to load and query the app database.",
  "windows": ["main"],
  "permissions": [
    "sql:default",
    "sql:allow-load",
    "sql:allow-execute",
    "sql:allow-select",
    "sql:allow-close"
  ]
}
